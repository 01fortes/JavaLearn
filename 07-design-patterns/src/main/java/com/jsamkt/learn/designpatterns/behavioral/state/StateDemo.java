package com.jsamkt.learn.designpatterns.behavioral.state;

/**
 * Demonstrates the State pattern.
 * The State pattern allows an object to alter its behavior when its internal state
 * changes, appearing to change its class.
 */
public class StateDemo {
    
    public static void demo() {
        System.out.println("\n# State Pattern");
        System.out.println("The State pattern allows an object to alter its behavior when its internal state changes.");
        System.out.println("Example implementation will be added in future updates.");
    }
}